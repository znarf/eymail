const components = {};

components.container = require('./container');

components.baseBlock = require('./baseBlock');

components.centeredBlock = require('./centeredBlock');

components.block = require('./block');

components.button = require('./button');

components.cta = require('./cta');

components.divider = require('./divider');

components.image = require('./image');

components.section = require('./section');

components.text = require('./text');

components.stylesheet = require('./stylesheet');

module.exports = components;
